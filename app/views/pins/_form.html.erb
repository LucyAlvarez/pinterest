<%= form_with(model: pin, local: true) do |form| %>
  <% if pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% pin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group">
  <div class="field">
    <%= form.label "Fotografia" %>
    <%= form.text_field :photo, id: :pin_photo, :class=>"form-control", :placeholder=>"Link" %>
  </div>
</div>
<div class="form-group">
  <div class="field">
    <%= form.label "Descripcion" %>
    <%= form.text_field :description, id: :pin_description, :class=>"form-control", :placeholder=>"Descripcion" %>
  </div>
</div>
  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
